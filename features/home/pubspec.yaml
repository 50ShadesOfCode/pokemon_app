name: home
description: home feature for pokemon app.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_dependencies:
    path: ../../shared_dependencies
  core:
    path: ../../core
  router:
    path: ../../router
  domain:
    path: ../../domain
  details:
    path: ../details
  core_ui:
    path: ../../core_ui
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
