name: core
description: Core module for pokemon app.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  router:
    path: ../router
  shared_dependencies:
    path: ../shared_dependencies
  data:
    path: ../data
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
